# sonic-ws

Async Python client and server for the SonicWS schema-driven binary WebSocket protocol. Encoding, validation, batching, and bounded raw-DEFLATE compression use the shared Rust core.

See the repository’s [`docs/py`](../../docs/py/README.md) directory for installation, packet schemas, clients, servers, middleware, browser assets, packaging, and compatibility details.
